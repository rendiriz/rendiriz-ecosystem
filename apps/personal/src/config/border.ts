export const borderTop = (color: string) => {
  return { borderTop: `1px solid ${color}` };
};

export const borderRight = (color: string) => {
  return { borderRight: `1px solid ${color}` };
};

export const borderBottom = (color: string) => {
  return { borderBottom: `1px solid ${color}` };
};

export const borderLeft = (color: string) => {
  return { borderLeft: `1px solid ${color}` };
};
